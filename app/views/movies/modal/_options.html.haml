.modal-options
  = form_for([current_user, @movie_decorator.watchlist], remote: true) do |f|
    = f.hidden_field(:movie_id, value: @movie_decorator.id)
    = f.submit "Movie", type: :image, src: url_to_image(@movie_decorator.watchlist_image), class: "pull-right", id:"watchlist-button"